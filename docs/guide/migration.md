# 迁移指南

## 从 Element Plus 迁移

- 组件标签前缀由 `el-` 改为 `al-`，例如 `<el-button>` → `<al-button>`。
- 核心 Props（`type` / `size` / `loading` / `disabled` / `nativeType`）保持使用心智一致。
- 部分组件 API 仍在补齐中，请以各组件文档页为准。

## 从 Ant Design Vue 迁移

- 命名风格差异较大，建议按组件逐个对照 `docs/components/*` 文档替换。
